## 2024-05-30 - Sliding Window Optimization for Raid Detection
**Learning:** Utilizing Array.filter inside a loop to find patterns based on temporal closeness in sorted arrays degrades performance to O(N^2).
**Action:** Utilize a two-pointer sliding window to strictly bound operations to O(N).
